# Запросы в Claude Code — Запуск проекта

## ЗАПРОС 1 — Исправить оплату (сделать первым)

Вставь это в Claude Code:

```
Прочитай CLAUDE.md и PROJECT_AUDIT_REPORT.md.
Используй skill: payment-debug-fix

Проблема: при нажатии "Купить" показывает "Не удалось провести оплату".

Выполни по шагам:

1. Открой lib/yookassa.ts — покажи мне текущую логику проверки ключей
2. Открой app/api/payment/yookassa/route.ts — покажи error handling
3. Исправь: если YUKASSA_SHOP_ID пустой ИЛИ === 'test' → использовать mock
4. Исправь: если ЮКасса вернул ошибку → fallback на mock (не показывать "Ошибку")
5. Mock flow: после "оплаты" должно появляться "Тестовая оплата прошла, план активирован"

После исправления дай команды:
- как проверить локально
- как задеплоить на Vercel
```

---

## ЗАПРОС 2 — Полный QA аудит

```
Используй skill: qa-full-audit

Проверь весь проект и составь таблицу:
| Функция | Статус | Что исправить |
|---------|--------|---------------|
| Генерация КП | ✅/❌ | ... |
| PDF скачивание | ✅/❌ | ... |
| Оплата mock | ✅/❌ | ... |
| Magic link auth | ✅/❌ | ... |
| Dashboard история | ✅/❌ | ... |
| Мобильный UX | ✅/❌ | ... |
| Лимиты (защита от обхода) | ✅/❌ | ... |

Начни с чтения кода, не угадывай.
Открой: app/page.tsx, app/result/page.tsx, lib/credits.ts, app/api/generate/route.ts
```

---

## ЗАПРОС 3 — UX готовность к рекламе

```
Используй skill: ux-launch-ready

Проверь UX каждого экрана:
1. Лендинг — что видит пользователь первые 5 секунд?
2. Форма — понятны ли все поля без объяснений?
3. /result — понятно как скачать PDF?
4. Paywall — понятна разница между тарифами?

Составь список из ТОП-5 правок которые повысят конверсию.
Отсортируй по: влияние на конверсию × сложность реализации.
```

---

## ЗАПРОС 4 — Защита монетизации от обхода

```
Используй skill: payment-debug-fix

Добавь серверную проверку в /api/generate/route.ts:
1. Получить userId из cookie/сессии
2. Проверить план в Vercel KV (не в localStorage)
3. Если free и использовано >= 3 → вернуть { error: 'limit_reached', code: 403 }
4. Декрементировать счётчик в KV при успешной генерации

Пользователь не должен иметь возможность обойти лимит через DevTools.
```

---

## ЗАПРОС 5 — Smoke test перед запуском

```
Используй skill: qa-full-audit

Я собираюсь запустить рекламу. Проведи финальную проверку:

1. Запусти все тесты: npx playwright test
2. Если тесты падают — исправь
3. Проверь npm run build — должно быть 0 ошибок
4. Составь финальный список: что готово, что НЕ готово

Скажи мне честно: можно запускать рекламу или нет?
Если нет — что нужно исправить за 1-2 часа?
```
